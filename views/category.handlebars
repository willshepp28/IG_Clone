

<div class="container">
    <div class="row">
        {{#category}}
        <h1 class="text-center">{{category_name}}</h1>
        {{/category}}
    </div>
</div>
<div class="container">
    <div class="row">
        {{#each post}}
        <div class="col-md-4">
            <img src="{{photo}}"> </div>

        {{/each}}
    </div>
</div>